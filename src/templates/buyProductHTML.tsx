import * as React from "react";
import { Buy } from "../../src/schemas/types/buy";

export default function showBuyProductHTML(buy: Buy) {
  console.log("called Auth HTML");

  return (
    <>
      {/* Buy Page */}
      .<br />
      <h1>Buy Page</h1>
      <br />
    </>
  );
}